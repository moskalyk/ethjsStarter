{
  "name": "ethjsstarter",
  "version": "0.0.1",
  "description": "Example project using EthereumJS as a git submodule",
  "main": "index.js",
  "scripts": {
    "postinstall": "git submodule init && git submodule update && npm run build:modules",
    "build:modules": "cd ethereumjs-monorepo && npm i",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ethereumjs/common": "^4.2.0",
    "@ethereumjs/tx": "^5.2.1",
    "@ethereumjs/vm": "^7.2.1",
    "ethereumjs-abi": "^0.6.8",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "tsx": "^4.7.1"
  }
}
